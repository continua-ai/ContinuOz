services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: oz
      POSTGRES_PASSWORD: oz
      POSTGRES_DB: oz_workspace
    ports:
      - "5432:5432"
    volumes:
      - oz-postgres-data:/var/lib/postgresql/data

volumes:
  oz-postgres-data:
